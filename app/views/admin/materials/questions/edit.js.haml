- question_edit_form=render @partial

$(".question-row-#{escape_javascript("#{@question.id}")} .accordion-inner div").remove();
$(".question-row-#{escape_javascript("#{@question.id}")} .accordion-inner").append("<div class=\"row well\">#{escape_javascript(question_edit_form)}</div>");
